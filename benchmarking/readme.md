This folder contains the results of benchmarking experiments reported in the paper. Each zip archive contains results for a particular type of data. The results are saved in files with the names 'res_n_i.mat', where n is the input size and i is the input instance number.

runSDP.m : script to reproduce computational experiments workflow.
